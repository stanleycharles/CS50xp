#include <cs50.h> // not working
#include <stdio.h>

int main(void)
{
  // Prompt user for x
  int x = get_int("x: ");

  // Prompt user for y
  int y = get_int("y: ");

  // Prompt addition
  printf("%i\n", x + y);m  
}